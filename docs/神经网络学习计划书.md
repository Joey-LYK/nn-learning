> **框架更新说明**：本项目代码已从单片式重构为企业级分层架构。
> - 旧版单片代码 `ui/docs/example/`（行号引用指向此处）
> - 新版框架 `nn_framework/`：`core/`(计算内核) → `trainer/`(训练引擎) → `apps/demo/`(应用) → `libs/`(第三方库)
> - **`nn.c:{line}` 行号指向旧版文件**，概念对应新版 `core/nn.c` 中同名函数
# 神经网络学习计划书

> **定位**：面向嵌入式软件工程师，从高中数学起点循序渐进，过渡到工程实践。
>
> **核心原则**：先动手建立直觉，再补数学理解，最后回归工程落地。
>
> **教学载体**：本项目中自研的 `nn.c` 神经网络库 + `train_demo.exe` 交互式训练程序。

---

## 文档结构

```
docs/
├── 神经网络学习计划书.md              ← 你正在看的这份（大纲 + 路线图）
├── 01-基础版-直觉与入门.md           ← 建立直觉，跑通 demo
├── 02-进阶版-原理与推导.md           ← 数学推导，逐行读 nn.c
├── 03-深度版-工程与实践.md           ← 优化部署，嵌入式落地
└── 04-深度进阶版-前沿架构与拓展.md   ← 突破 MLP，接触 CNN/RNN/注意力机制
```

---

## 学习路线总览

```
基础版（直觉）──→ 进阶版（原理）──→ 深度版（工程）──→ 深度进阶版（前沿）
     │                 │                 │                 │
     ├─ 跑 train_demo   ├─ 导数与梯度     ├─ Adam 优化算法   ├─ CNN 基础
     ├─ 改参数看效果    ├─ 链式法则       ├─ 过拟合/正则化   ├─ RNN / LSTM
     ├─ 比喻理解神经元  ├─ 反向传播推导   ├─ 数据采集策略    ├─ 注意力机制
     ├─ 前向传播直觉    ├─ 损失函数数学   ├─ 模型序列化部署   ├─ 迁移学习
     └─ nn.c 前半部分   ├─ nn.c 后半部分  ├─ 嵌入式量化优化  ├─ 模型压缩（剪枝/蒸馏）
                         └─ 数值稳定性    └─ 在线学习       └─ 嵌入式 AI 框架
```

**预计总学时**：基础版 1 周 / 进阶版 2~3 周 / 深度版 2 周 / 深度进阶版 3~4 周

---

## 基础版：直觉与入门

> **目标**：零数学门槛，通过动手和比喻理解神经网络"在做什么"。
> **前提**：高中数学（函数概念、基本代数运算）。
> **产出**：能读懂 `nn.c` 的前向传播部分，能修改 `train_demo.c` 的网络结构和参数。

### 知识点大纲

#### 1. 先跑起来——第一次体验

| 编号 | 知识点 | 核心问题 | 对应项目代码 |
|------|--------|---------|------------|
| B-01 | 运行 train_demo.exe | 这个程序在做什么？ | `train_demo.c main()` |
| B-02 | 观察训练过程 | Loss 在下降意味着什么？ | `train_task()` 打印逻辑 |
| B-03 | 观察预测结果 | 网络学到了什么？ | 验证阶段的预测对比输出 |

**动手任务**：分别跑 sin(x)、XOR、抛体运动三个任务，记录各自的 Loss 收敛情况。

#### 2. 用比喻理解核心概念

| 编号 | 知识点 | 核心问题 | 比喻 |
|------|--------|---------|------|
| B-04 | 什么是神经元 | 一个神经元在算什么？ | 调音台上的旋钮 |
| B-05 | 什么是权重和偏置 | w 和 b 分别控制什么？ | 旋钮的灵敏度和零点 |
| B-06 | 什么是激活函数 | 为什么要做非线性变换？ | 阈值开关（"够了就通过"） |
| B-07 | 什么是前向传播 | 数据从输入到输出经历了什么？ | 流水线上的加工工序 |
| B-08 | 什么是损失函数 | 怎么衡量"预测得有多差"？ | 考试得分与标准答案的差距 |

#### 3. 对着代码理解前向传播

| 编号 | 知识点 | 核心问题 | 对应项目代码 |
|------|--------|---------|------------|
| B-09 | 网络的创建 | `nn_create` 和 `nn_add_layer` 在做什么？ | `nn.c` 第 89~145 行 |
| B-10 | 权重初始化 | 为什么不能全部初始化为 0？ | `nn.c` 第 128~137 行（Xavier 初始化） |
| B-11 | 前向传播的计算 | `nn_forward` 里那两个循环在算什么？ | `nn.c` 第 173~224 行 |
| B-12 | 网络结构的设计 | 隐藏层、神经元数量怎么选？ | `train_demo.c` 各任务的参数 |

#### 4. 改参数做实验

| 编号 | 知识点 | 实验内容 |
|------|--------|---------|
| B-13 | 学习率的影响 | 改 `lr` 为 0.001 / 0.01 / 0.1，对比收敛速度 |
| B-14 | 网络深度的影响 | 给 sin(x) 任务分别用 1层/2层/4层 隐藏层 |
| B-15 | 网络宽度的影响 | 同样层数，神经元从 4 改到 32 / 128 |
| B-16 | 训练轮次的影响 | epochs 从 100 改到 10000，观察是否过拟合 |
| B-17 | XOR 的特殊性 | 去掉 XOR 的隐藏层，看为什么学不会 |

---

## 进阶版：原理与推导

> **目标**：理解神经网络背后的数学原理，能对着 `nn.c` 逐行解释每一行的数学含义。
> **前提**：基础版完成 + 高中导数概念（知道 f'(x) 表示变化率）。
> **产出**：能手推反向传播公式，理解 nn.c 中梯度计算的每一步。

### 知识点大纲

#### 1. 最小化数学——为神经网络准备的微积分

| 编号 | 知识点 | 核心问题 | 学习方式 |
|------|--------|---------|---------|
| M-01 | 导数的直觉 | f'(x) 究竟在说什么？ | 图形化理解：切线斜率 = 变化率 |
| M-02 | 常见函数的导数 | x²、eˣ、1/(1+e⁻ˣ) 的导数怎么求？ | 记忆三个核心结果 |
| M-03 | 偏导数 | 多变量时怎么求导？ | "只盯一个变量，其他当常数" |
| M-04 | 梯度 | 梯度指向哪里？ | "最陡的下坡方向"——山丘类比 |
| M-05 | 链式法则 | 复合函数怎么求导？ | "洋葱层层剥"——从外到内逐层求导 |

**注意**：不讲 ε-δ 定义、不讲积分、不讲多变量泰勒展开。只学"用得到的那点微积分"。

#### 2. 损失函数的数学

| 编号 | 知识点 | 核心问题 | 对应项目代码 |
|------|--------|---------|------------|
| M-06 | MSE 的定义和梯度 | 为什么 Loss = Σ(ŷ-y)²？对 ŷ 的导数是什么？ | `nn.c` 第 273~278 行 |
| M-07 | 交叉熵的定义 | 分类问题为什么不用 MSE？ | `nn.c` 第 260~263 行 |
| M-08 | 损失函数的梯度指向 | 梯度怎么告诉权重"该往哪调"？ | 与参数更新方向的关系 |

#### 3. 反向传播——核心推导

| 编号 | 知识点 | 核心问题 | 对应项目代码 |
|------|--------|---------|------------|
| M-09 | 前向传播的数学表达 | z = Wx + b, a = f(z) | `nn.c` 第 184~215 行 |
| M-10 | 输出层误差 δ | δ_L = (ŷ - y) × f'(z) | `nn.c` 第 248~279 行 |
| M-11 | 误差反向传播 | δ_l = (W^(l+1))^T × δ_(l+1) × f'(z_l) | `nn.c` 第 282~298 行 |
| M-12 | 权重梯度 | ∂L/∂W = δ × a^T，为什么是这样？ | `nn.c` 第 332~342 行 |
| M-13 | 偏置梯度 | ∂L/∂b = δ，为什么这么简单？ | `nn.c` 第 345 行 |
| M-14 | 梯度下降更新 | w_new = w_old - lr × gradient | `nn.c` 第 320~348 行 |
| M-15 | 一步训练的完整流程 | forward → loss → backward → update | `nn.c` 第 356~364 行 |

**核心目标**：能从数学公式出发，逐行对应到 `nn.c` 的代码，做到"公式 ↔ 代码"双向翻译。

#### 4. 激活函数的数学细节

| 编号 | 知识点 | 核心问题 | 对应项目代码 |
|------|--------|---------|------------|
| M-16 | Sigmoid 及其导数 | σ'(z) = σ(z)(1-σ(z))，为什么恰好是这个？ | `nn.c` 第 48 行 |
| M-17 | ReLU 及其导数 | 为什么 ReLU 能缓解梯度消失？ | `nn.c` 第 50 行 |
| M-18 | 梯度消失的数学原因 | σ'(z) ≤ 0.25 → 多层相乘趋近于 0 | `nn.c` 第 269~275 行 与第 381~383 行 |
| M-19 | Xavier 初始化的数学推导 | 为什么 Var(W) = 2/(n_in + n_out)？ | `nn.c` 第 128 行 |

#### 5. 数值稳定性

| 编号 | 知识点 | 核心问题 | 对应项目代码 |
|------|--------|---------|------------|
| M-20 | Softmax 溢出问题 | 为什么先减去 max 值？ | `nn.c` 第 201~214 行 |
| M-21 | log(0) 防护 | 交叉熵中 1e-10 的作用 | `nn.c` 第 262 行 |
| M-22 | 梯度裁剪的必要性 | 梯度爆炸时怎么办？ | 进阶版拓展内容 |

---

## 深度版：工程与实践

> **目标**：从"能训练"到"能落地"——掌握工程实践中会遇到的真实问题。
> **前提**：进阶版完成 + 有基本的嵌入式开发经验。
> **产出**：能将训练好的模型部署到资源受限的嵌入式平台上运行。

### 知识点大纲

#### 1. 优化算法进阶

| 编号 | 知识点 | 核心问题 |
|------|--------|---------|
| E-01 | SGD 的缺陷 | 固定学习率有什么问题？震荡、收敛慢 |
| E-02 | 动量法 | "惯性"怎么帮助优化？物理类比 |
| E-03 | Adam 算法 | 为什么 Adam 是深度学习的事实标准？ |
| E-04 | 学习率调度 | 为什么后期要降低学习率？ |
| E-05 | 优化算法选型指南 | 什么场景用什么优化器？ |

#### 2. 泛化能力与过拟合

| 编号 | 知识点 | 核心问题 |
|------|--------|---------|
| E-06 | 过拟合与欠拟合 | 怎么判断模型是"背答案"还是"真学会"？ |
| E-07 | 训练集 / 验证集 / 测试集 | 三个数据集各自的职责 |
| E-08 | L2 正则化 | 为什么要"惩罚"大权重？ |
| E-09 | Dropout | 随机"关闭"神经元为什么能防止过拟合？ |
| E-10 | 早停法 | 训练到什么时候该停？ |

#### 3. 数据工程

| 编号 | 知识点 | 核心问题 |
|------|--------|---------|
| E-11 | 数据采集策略 | 没有仿真时，真实数据怎么来？ |
| E-12 | 数据归一化 | 为什么要除以 200？不归一化会怎样？ |
| E-13 | 数据增强 | 数据不够怎么办？ |
| E-14 | 在线学习 vs 离线学习 | 先采集再训练 vs 边跑边学，各适合什么场景？ |
| E-15 | 仿真预训练 + 真实微调 | 你已经在用的策略，如何系统化？ |

#### 4. 模型序列化与部署

| 编号 | 知识点 | 核心问题 | 对应项目代码 |
|------|--------|---------|------------|
| E-16 | 模型导出格式设计 | `nn_export` 的二进制格式怎么设计的？ | `nn.c` 第 460~515 行 |
| E-17 | 模型导入与验证 | `nn_import` 如何校验数据完整性？ | `nn.c` 第 517~575 行 |
| E-18 | 版本兼容性 | 模型格式升级时如何兼容旧模型？ |
| E-19 | 文件 I/O 与 Flash 存储 | 嵌入式平台如何持久化模型？ | `main.c` 的 `PX_AnnSaveToFile` |

#### 5. 嵌入式部署优化

| 编号 | 知识点 | 核心问题 |
|------|--------|---------|
| E-20 | 参数量与内存估算 | 一个网络需要多少 RAM？ |
| E-21 | 推理延迟估算 | 一次前向传播需要多少 FLOPs？ |
| E-22 | 浮点 vs 定点 | float32、float16、int8 各有什么取舍？ |
| E-23 | 查找表加速激活函数 | 用 LUT 替换 expf() 计算 |
| E-24 | 内存池管理 | 嵌入式平台如何避免频繁 malloc/free？ |
| E-25 | SIMD 指令加速 | ARM DSP / NEON 指令加速矩阵运算 |

#### 6. 端到端项目实战

| 编号 | 知识点 | 核心问题 |
|------|--------|---------|
| E-26 | 需求分析 | 什么问题适合用神经网络？什么不适合？ |
| E-27 | 数据准备流程 | 从原始传感器数据到训练样本 |
| E-28 | 模型开发迭代 | 架构设计 → 训练 → 评估 → 修改 → 循环 |
| E-29 | 模型压缩 | 在保证精度的前提下减小模型体积 |
| E-30 | 部署验证 | 部署后的精度如何验证？回归测试 |

---

## 深度进阶版：前沿架构与拓展

> **目标**：突破 MLP（多层感知器）的局限，接触工业界主流网络架构，为嵌入式 AI 的更多应用场景储备知识。
> **前提**：深度版完成 + 对嵌入式开发有实践经验。
> **产出**：能理解 CNN/RNN/注意力机制的核心思想，能评估"这个问题该用什么网络架构"，了解嵌入式 AI 框架和模型压缩技术。

### 知识点大纲

#### 1. 从 MLP 到 CNN——处理空间结构化数据

| 编号 | 知识点 | 核心问题 |
|------|--------|---------|
| A-01 | MLP 处理图像/信号的局限 | 全连接层为什么不适合处理空间数据？参数爆炸、丢失空间信息 |
| A-02 | 卷积操作的直觉 | 卷积核在图像上"滑动扫描"在做什么？边缘检测类比 |
| A-03 | 卷积层的三个核心概念 | 卷积核（filter）、步长（stride）、填充（padding） |
| A-04 | 池化层 | 为什么要"缩小"特征图？降维、平移不变性 |
| A-05 | CNN 在嵌入式中的典型应用 | 传感器信号的一维卷积、工业缺陷检测的二维卷积 |

#### 2. 从 MLP 到 RNN——处理时间序列数据

| 编号 | 知识点 | 核心问题 |
|------|--------|---------|
| A-06 | 时序数据的特点 | 为什么 MLP 无法处理"上一步的结果影响下一步"？ |
| A-07 | RNN 的核心思想 | 隐藏状态 h_t：网络的"短期记忆" |
| A-08 | 梯度消失在 RNN 中的放大效应 | 为什么 RNN 的梯度消失比 MLP 更严重？ |
| A-09 | LSTM 的门控机制 | 遗忘门、输入门、输出门分别控制什么？ |
| A-10 | 嵌入式中的 RNN 应用场景 | 电机故障预测、传感器数据时序分析、语音唤醒词 |

#### 3. 注意力机制与 Transformer 基础

| 编号 | 知识点 | 核心问题 |
|------|--------|---------|
| A-11 | 注意力的直觉 | 人类阅读时"关注重点"的机制，如何用数学表达？ |
| A-12 | Q / K / V 的含义 | 查询、键、值分别是什么角色？图书馆检索类比 |
| A-13 | 自注意力机制 | 一个序列内部各元素如何"互相关注"？ |
| A-14 | Transformer 的核心结构 | 编码器-解码器架构概览 |
| A-15 | Transformer 在嵌入式中的可行性分析 | 计算量、内存需求、模型大小——资源受限平台能否跑得动？ |

#### 4. 迁移学习与预训练模型

| 编号 | 知识点 | 核心问题 |
|------|--------|---------|
| A-16 | 迁移学习思想 | "在别人的模型基础上微调"为什么比从头训练高效？ |
| A-17 | 预训练 + 微调范式 | 预训练学通用特征，微调学特定任务 |
| A-18 | 特征提取 vs 端到端微调 | 两种迁移策略的取舍 |
| A-19 | 嵌入式中的迁移学习实践 | 用 PC 训练，部署到 MCU 的完整流程 |

#### 5. 模型压缩技术

| 编号 | 知识点 | 核心问题 |
|------|--------|---------|
| A-20 | 模型剪枝 | 去掉"不重要"的权重，网络变小但精度不掉多少？ |
| A-21 | 知识蒸馏 | "大模型教小模型"——如何用大网络的输出训练小网络？ |
| A-22 | 量化感知训练（QAT） | 在训练时就模拟低精度，比训练后量化精度更高 |
| A-23 | 结构化压缩 | 深度可分离卷积、MobileNet 等轻量架构思想 |
| A-24 | 压缩策略选型 | 什么场景用什么压缩方法？精度 vs 速度的权衡 |

#### 6. 嵌入式 AI 框架与工具链

| 编号 | 知识点 | 核心问题 |
|------|--------|---------|
| A-25 | TFLite Micro 架构 | Google 的嵌入式推理框架，解释器 + 运算内核 |
| A-26 | ONNX 与模型转换 | 用中间格式打通不同训练框架和部署平台 |
| A-27 | CMSIS-NN | ARM 官方的神经网络推理库，硬件加速 API |
| A-28 | 自研推理引擎的设计 | 你已经有的 nn.c 如何扩展成更通用的推理引擎？ |
| A-29 | 训练框架 vs 推理框架 | PyTorch/TensorFlow 负责"教"，轻量库负责"用" |

#### 7. 安全、鲁棒性与可靠性

| 编号 | 知识点 | 核心问题 |
|------|--------|---------|
| A-30 | 对抗样本攻击 | 给输入加一个"人眼看不到"的扰动，模型就输出错误结果？ |
| A-31 | 输入异常检测 | 传感器数据异常时，网络输出不可信怎么办？ |
| A-32 | 模型退化监控 | 部署后模型精度随时间下降，如何自动检测？ |
| A-33 | 安全关键场景的验证 | 功能安全标准（ISO 26262 等）对神经网络的要求 |

---

## 版本之间的关系

```
基础版              进阶版              深度版              深度进阶版
─────               ─────               ─────               ──────────
"在做什么"     →  "为什么这样做"   →  "怎样落地"     →  "还有哪些可能"
                                                 
跑通 demo       →   手推反向传播     →   部署到嵌入式  →   了解 CNN/RNN/Transformer
改参数做实验     →   逐行读 nn.c      →   优化推理性能   →   评估不同架构的适用场景
比喻理解概念     →   公式 ↔ 代码      →   解决工程问题    →   模型压缩与前沿技术

不需要微积分     →   导数+链式法则     →   完整理解+经验   →   线性代数+概率论基础
```

---

## 学完之后你应该能做到

| 能力 | 基础版 | 进阶版 | 深度版 | 深度进阶版 |
|------|--------|--------|--------|------------|
| 跑通 train_demo 并理解输出 | ✅ | ✅ | ✅ | ✅ |
| 修改网络结构和超参数 | ✅ | ✅ | ✅ | ✅ |
| 用比喻向别人解释神经网络 | ✅ | ✅ | ✅ | ✅ |
| 手推反向传播公式 | | ✅ | ✅ | ✅ |
| 逐行解释 nn.c 的每一行代码 | | ✅ | ✅ | ✅ |
| 发现并修复训练问题（不收敛/过拟合） | | ✅ | ✅ | ✅ |
| 选择合适的优化算法和超参数 | | | ✅ | ✅ |
| 从零采集数据训练一个实用模型 | | | ✅ | ✅ |
| 将模型部署到嵌入式平台 | | | ✅ | ✅ |
| 对模型做量化和性能优化 | | | ✅ | ✅ |
| 根据问题类型选择合适的网络架构（MLP/CNN/RNN） | | | | ✅ |
| 理解 Transformer / 注意力机制的核心思想 | | | | ✅ |
| 使用模型压缩技术（剪枝/蒸馏/量化） | | | | ✅ |
| 评估嵌入式 AI 框架的适用性 | | | | ✅ |

---

## 参考代码索引

学习过程中反复对照的项目源文件：

| 文件 | 作用 | 在学习计划中的角色 |
|------|------|-------------------|
| `nn.h` | 数据结构定义 | 基础版开始阅读，理解网络的数据表示 |
| `nn.c` | 核心算法实现 | **基础版~深度版的核心教材**，逐行精读 |
| `train_demo.c` | 交互式训练程序 | 基础版的实验平台 |
| `task_sin.c` | sin(x) 拟合任务 | 最简单的入门案例 |
| `task_xor.c` | XOR 逻辑门任务 | 理解非线性的必要性 |
| `task_projectile.c` | 抛体运动预测任务 | 理解归一化和回归 |
| `main.c` | PainterEngine GUI 版本 | 深度版的在线学习参考 |

**深度进阶版**不依赖本项目代码（项目是纯 MLP 架构），将以对比分析 + 概念讲解 + 框架实践为主。
